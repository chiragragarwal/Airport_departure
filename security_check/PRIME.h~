
int isprime(int num)
{
	int i = 2;	//as 2 is the first prime number
    while(i*i <= num)  	//as good as comparing i < sqrt(num)
    {
        if(num % i == 0)
            return -1;		//Not a prime number, return the factor
        i++;
    }
    return 1;				//Prime number
}
